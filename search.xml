<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[cuda学习笔记2]]></title>
    <url>%2Fcuda-practice2.html</url>
    <content type="text"><![CDATA[本文主要介绍如何用cuda进行并行计算，还有一个有趣的实验：生成julia分形图。 本节要点 一种错误处理的宏定义 CUDA实现并行性1.基于GPU的矢量求和看代码： 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475#include "cuda_runtime.h"#include "device_launch_parameters.h"#include &lt;stdio.h&gt;#include &lt;math.h&gt;#include &lt;helper_cuda.h&gt;using namespace std;#define N 100__global__ void add_kernel(double *a, double *b, double *c) &#123; int tid = blockIdx.x; if (tid &lt; N) &#123; c[tid] = a[tid] + b[tid]; &#125; &#125;__global__ void value_init_kernel(double *a, double *b) &#123; int tid = blockIdx.x; if (tid &lt; N) &#123; a[tid] = 1.0*tid; b[tid] = (1.0*tid*tid); &#125;&#125;int main(void)&#123; cudaError_t err1 = cudaSuccess, err2 = cudaSuccess, err3 = cudaSuccess; double a[N], b[N], c[N]; double *dev_a, *dev_b, *dev_c; err1 = cudaMalloc((void**)&amp;dev_a, N * sizeof(double)); err2 = cudaMalloc((void**)&amp;dev_b, N * sizeof(double)); err3 = cudaMalloc((void**)&amp;dev_c, N * sizeof(double)); if (err1 != cudaSuccess || err2 != cudaSuccess || err3 != cudaSuccess) &#123; fprintf(stderr, "Failed to allocate device value (error code (%s,%s,%s))!\n", cudaGetErrorString(err1), cudaGetErrorString(err2), cudaGetErrorString(err3)); exit(EXIT_FAILURE); &#125; value_init_kernel &lt;&lt;&lt;N, 1 &gt;&gt;&gt; (dev_a, dev_b);////在GPU上赋值操作 add_kernel &lt;&lt;&lt;N, 1 &gt;&gt;&gt; (dev_a, dev_b, dev_c)iii;////在GPU上相加操作 err1 = cudaMemcpy(a, dev_a, N * sizeof(double), cudaMemcpyDeviceToHost); err2 = cudaMemcpy(b, dev_b, N * sizeof(double), cudaMemcpyDeviceToHost); err3 = cudaMemcpy(c, dev_c, N * sizeof(double), cudaMemcpyDeviceToHost); if (err1 != cudaSuccess || err2 != cudaSuccess || err3 != cudaSuccess) &#123; fprintf(stderr, "Failed to copy device value to host value (error code (%s,%s,%s))!\n", cudaGetErrorString(err1), cudaGetErrorString(err2), cudaGetErrorString(err3)); exit(EXIT_FAILURE); &#125; for (int i = 0; i &lt; N; i++) &#123; printf("%f + %f = %f\n", a[i], b[i], c[i]); &#125; ////释放GPU内存 cudaFree(dev_a); cudaFree(dev_b); cudaFree(dev_c); return 0; &#125; 代码实现在GPU上对变量赋值，然后相加返回给主机函数，上面每次对错误处理的代码太冗长了，可以用一个宏定义来简化： 12345678910static void HandleError( cudaError_t err, const char *file, int line ) &#123; if (err != cudaSuccess) &#123; printf( "%s in %s at line %d\n", cudaGetErrorString( err ), file, line ); exit( EXIT_FAILURE ); &#125;&#125;#define HANDLE_ERROR( err ) (HandleError( err, __FILE__, __LINE__ )) julia分形图案最后是一个有趣的例子：Julia分形图案： 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100101102103104105106107108109110111112113114115116117118119#include &lt;stdio.h&gt;#include "cuda_runtime.h"#include "device_launch_parameters.h"#include "../common/book.h" ////GPU高性能编程CUDA实战代码#include "../common/image.h"#include "book.h"#include "image.h"#define DIM1 5760#define DIM2 5760 //每一维度的长度#define iter_N 200struct cuComplex &#123; float r; float i; __device__ cuComplex(float a, float b) :r(a), i(b) &#123;&#125; __device__ float magnitude2(void) &#123; return r*r + i*i; &#125; ////返回复数的模的平方 __device__ cuComplex operator*(const cuComplex&amp; a) &#123; return cuComplex(r*a.r - i*a.i, i*a.r + r*a.i); &#125; __device__ cuComplex operator+(const cuComplex&amp; a) &#123; return cuComplex(r + a.r, i + a.i); &#125;&#125;;__device__ int julia(int x, int y) &#123; const float scale = 1.5; float jx = scale * (float)(DIM1 / 2 - x) / (DIM1/ 2); float jy = scale * (float)(DIM2 / 2 - y) / (DIM2 / 2); cuComplex c(-0.8, 0.156); //-0.8,0.156; cuComplex a(jx, jy); for (int i = 1; i &lt; iter_N; i++) &#123; a = a * a + c; if (a.magnitude2() &gt; 1000) return i; &#125; return 0;&#125;__global__ void kernel(unsigned char *ptr) &#123; int x = blockIdx.x; int y = blockIdx.y; int offset = x + y * gridDim.x; int juliaValue = julia(x, y); ////美工部分。。。。。。 if (juliaValue ==0) &#123; ptr[offset * 4 + 0] = 0; ptr[offset * 4 + 1] = 0; ptr[offset * 4 + 2] = 0; ptr[offset * 4 + 3] = 255; &#125; if (juliaValue &lt; 90 &amp;&amp; juliaValue &gt;= 1) &#123; ptr[offset * 4 + 0] = (int)(255 * juliaValue / (2.0 * iter_N)); ptr[offset * 4 + 1] = 0; ptr[offset * 4 + 2] = 0; ptr[offset * 4 + 3] = 255; &#125; if (juliaValue &lt; 120 &amp;&amp; juliaValue &gt;=90) &#123; ptr[offset * 4 + 0] = 255; ptr[offset * 4 + 1] = 255 - (int)(255 * juliaValue / (5.0 * iter_N)); ptr[offset * 4 + 2] = 255 - (int)(255 * juliaValue / (5.0 * iter_N)); ptr[offset * 4 + 3] = 255; &#125; if (juliaValue &lt; 180 &amp;&amp; juliaValue &gt;=120) &#123; ptr[offset * 4 + 0] = 10; ptr[offset * 4 + 1] = 215; ptr[offset * 4 + 2] = 200; ptr[offset * 4 + 3] = 255; &#125; if (juliaValue &lt;= 255 &amp;&amp; juliaValue &gt;=180) &#123; ptr[offset * 4 + 0] = (int)(255 * juliaValue / (1.0 * iter_N)); ptr[offset * 4 + 1] = 0; ptr[offset * 4 + 2] = 0; ptr[offset * 4 + 3] = 255; &#125; &#125;struct DataBlock &#123; unsigned char *dev_bitmap;&#125;;int main(void) &#123; DataBlock data; IMAGE bitmap(DIM1, DIM2); unsigned char *dev_bitmap; HANDLE_ERROR(cudaMalloc((void**)&amp;dev_bitmap, bitmap.image_size())); data.dev_bitmap = dev_bitmap; dim3 grid(DIM1, DIM2); ////实际上是DIM1*DIM2*1的三维线程格 kernel &lt;&lt; &lt;grid, 1 &gt;&gt; &gt; (dev_bitmap); HANDLE_ERROR(cudaMemcpy(bitmap.get_ptr(), dev_bitmap, bitmap.image_size(), cudaMemcpyDeviceToHost)); HANDLE_ERROR(cudaFree(dev_bitmap)); imwrite("C:/Users/Lenovo/Pictures/image/julia.png", bitmap.image); bitmap.show_image(); &#125;]]></content>
      <categories>
        <category>cuda</category>
      </categories>
      <tags>
        <tag>高性能编程</tag>
        <tag>cuda</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[cuda学习笔记1]]></title>
    <url>%2Fcuda-practice1.html</url>
    <content type="text"><![CDATA[本文主要介绍为主机编写代码和为设备编写代码的区别及细节。 本节目标 为主机（Host）编写代码和为设备（Device）编写代码的区别 从主机运行设备代码 如何在支持CUDA的设备上使用设备内存 如何查询系统中支持CUDA的设备信息 要点1.主机代码和设备代码 在GPU设备上执行的函数称为核函数，NVIDIA工具使用CUDA C编写核函数，CUDA C为标准C增加 global 修饰符，这个修饰符将告诉编译器，函数应该编译为设备而不是主机运行。具有该修饰符的函数将作为核函数交给CUDA编译器编译。 2.从主机运行设备代码如下所示： 123456789101112131415161718192021cudaError_t err = cudaSuccess; ////参数传递 int c; int *dev_c; err = cudaMalloc((void**)&amp;dev_c, sizeof(int)); add_kernel &lt;&lt; &lt;1, 1 &gt;&gt; &gt; (2, 7, dev_c); ////核函数处理 if (err != cudaSuccess) &#123; fprintf(stderr, "Failed to allocate device value dev_c (error code %s)!\n", cudaGetErrorString(err)); exit(EXIT_FAILURE); &#125; err = cudaMemcpy(&amp;c, dev_c, sizeof(int), cudaMemcpyDeviceToHost); ////主机代码中，不能对cudaMalloc()返回的指针进行读取，写入内存操作（即不可修改），但可以作参数传递，执行算术运算。 if (err != cudaSuccess) &#123; fprintf(stderr, "Failed to copy device value dev_c to c (error code %s)!\n", cudaGetErrorString(err)); exit(EXIT_FAILURE); &#125; printf("2+7=%d\n", c); cudaFree(dev_c); ////不可使用free()，因为在主机代码中不可修改该内存的值 ， 总的来说，主机指针只能访问主机代码的内存，设备指针只能访问设备代码的内存 从主机运行设备代码，以下几点值得注意： 1.可以像调用C函数那样将参数传递给核函数。2.当设备执行任何有用操作时，都需要分配内存，例如将计算值返回主机。 3.使用设备内存1.关于cudaMalloc()分配到的指针dev_c： 可以将dev_c传递给设备上执行的函数。可以在设备代码中使用dev_c进行内存读/写操作（修改该内存的值）。可以将dev_c传递给主机上执行的函数，但是不能在主机代码中对dev_c进行内存读/写操作。因为dev_c申请的是设备内存，不能在主机代码中用主机指针访问设备内存，反过来也一样，不能用设备指针访问主机内存。 2.利用cudaMencpy()在主机代码中访问设备内存，设备指针用完后用cudaFree()释放，不可以直接Free(),原因同上，不能在主机代码中直接访问设备内存。 4.查询系统中支持CUDA的设备信息1234567891011121314151617181920////查询设备 cudaDeviceProp prop; int count; err = cudaGetDeviceCount(&amp;count); for (int i = 0; i &lt; count; i++) &#123; err = cudaGetDeviceProperties(&amp;prop, i); if (err != cudaSuccess) &#123; fprintf(stderr, "Failed to get device %d properties (error code %s)!\n", i, cudaGetErrorString(err)); exit(EXIT_FAILURE); &#125; printf("Name: %s\n", prop.name);////这里打印设备信息，具体包含哪些信息查看《NVIDIA CUDA Programming Guide》 printf("Compute capability: %d . %d\n", prop.major,prop.minor); printf("Total global Mem:%lld\n", prop.totalGlobalMem); printf("Total constant Mem:%ld\n", prop.totalConstMem); &#125; 完整代码：12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061#include "cuda_runtime.h"#include "device_launch_parameters.h"#include &lt;stdio.h&gt;#include &lt;helper_cuda.h&gt;using namespace std;__global__ void add_kernel(int a,int b, int *c) &#123; *c = a + b;&#125;int main(void)&#123; cudaError_t err = cudaSuccess; ////参数传递 int c; int *dev_c; err = cudaMalloc((void**)&amp;dev_c, sizeof(int)); add_kernel &lt;&lt; &lt;1, 1 &gt;&gt; &gt; (2, 7, dev_c); ////核函数处理 if (err != cudaSuccess) &#123; fprintf(stderr, "Failed to allocate device value dev_c (error code %s)!\n", cudaGetErrorString(err)); exit(EXIT_FAILURE); &#125; err = cudaMemcpy(&amp;c, dev_c, sizeof(int), cudaMemcpyDeviceToHost); ////主机代码中，不能对cudaMalloc()返回的指针进行读取，写入内存操作（即不可修改），但可以作参数传递，执行算术运算。 if (err != cudaSuccess) &#123; fprintf(stderr, "Failed to copy device value dev_c to c (error code %s)!\n", cudaGetErrorString(err)); exit(EXIT_FAILURE); &#125; printf("2+7=%d\n", c); cudaFree(dev_c); ////不可使用free()，因为在主机代码中不可修改该内存的值 ， 总的来说，主机指针只能访问主机代码的内存，设备指针只能访问设备代码的内存 ////查询设备 cudaDeviceProp prop; int count; err = cudaGetDeviceCount(&amp;count); for (int i = 0; i &lt; count; i++) &#123; err = cudaGetDeviceProperties(&amp;prop, i); if (err != cudaSuccess) &#123; fprintf(stderr, "Failed to get device %d properties (error code %s)!\n", i, cudaGetErrorString(err)); exit(EXIT_FAILURE); &#125; printf("Name: %s\n", prop.name);////这里打印设备信息，具体包含哪些信息查看《NVIDIA CUDA Programming Guide》 printf("Compute capability: %d . %d\n", prop.major,prop.minor); printf("Total global Mem:%lld\n", prop.totalGlobalMem); printf("Total constant Mem:%ld\n", prop.totalConstMem); &#125; return 0; &#125;]]></content>
      <categories>
        <category>cuda</category>
      </categories>
      <tags>
        <tag>高性能编程</tag>
        <tag>cuda</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[标准库类型string笔记]]></title>
    <url>%2Fcpp-string.html</url>
    <content type="text"><![CDATA[本文主要介绍c++标准库中 string 的基本用法。 标准库类型string表示可变长字符序列，其定义在命名空间std中，使用时记得包含头文件： 1# include &lt;string&gt; string常用的操作1234567891011os&lt;&lt;s ////将s写入到输出流os中，返回osis&gt;&gt;s ////从is中读取字符串到s中，字符串用空白（空格符，换行，制表符）隔开，返回isgetline(is,s) ////从is中读取一行赋给s，返回iss.empty() ////判断s是否为空s.size() ////返回s的字符个数s[n] ////返回s中第n+1个字符的引用，使用时需要判断s是否为空！s1+s2 ////连接s1=s2 ////用s2的副本代替掉s1s1==s2 ////等性判断，对大小写敏感s1!=s2 ////等性判断，对大小写敏感&lt;,&lt;=,&gt;,&gt;= ////比较字符串大小关系，对大小写敏感 几个要点1. cin&gt;&gt;s和getline(cin,s)输入输出流最常用的还是标准库中的iostream，这里想说cin&gt;&gt;s和 getline(cin,s) 的区别： cin&gt;&gt;s 会忽略掉输入开头的空白，从第一个真正字符开始读起，直到遇见下一个空白为止（该空白没被读入）。 getline(cin,s)从给定输入流开始读取，直至遇到换行符结束，注意换行符也被读入，然后将读取的所有内容（除了最后的换行符外）放到s中。（s不包含换行符）。 2. s.size() 的返回类型 s.size() 是一个string::size_type类型，把它简单理解为一个无符号整形就好，但特殊的是该类型能够存放任何 string 对象的大小（理解为一个很大的值，手动狗头…） 3. auto和 decltypec++11标准中允许用auto和decltype推断一个变量的类型。使用auto必须初始化变量，比如： 12auto s; ////错误，s没初始化，这让编译器怎么推断...auto s = "hello"; ////正确，编译器推断"hello"的类型，并认为也是s的类型。 decltype可用于不初始化一个变量这种情况，这样用： 1decltype(s.size()) s1; 其实是编译器从 s.size() 中推断出一个类型，并认为s1也是该类型。 4.处理string对象中的字符 遍历处理对象中的字符串 c++11提供的范围for语句很好用，如下： 1234567string s("hello world!");for(auto &amp;c : s)&#123; c = toupper(c);&#125;cout&lt;&lt; s &lt;&lt;endl; 这里值得注意的是：若要改变string对象中字符的值，必须把循环变量定义成引用类型。 只处理一部分字符 可使用下标或者使用迭代器访问单个字符。 使用下标：下面的例子是将输入的一行字符串中第二个单词全部大写，最后输出。 123456789101112131415161718192021222324252627282930#include "stdafx.h"#include &lt;iostream&gt;#include&lt;string&gt;using namespace std;////利用下标运算符处理特定位置的部分字符串int main()&#123; string line1; getline(cin, line1); /////获取输入第一行 if (!line1.empty() &amp;&amp; !isspace(line1[0])) &#123; int word_count = 0; for (decltype(line1.size())index = 0; index != line1.size(); index++) &#123; if (isspace(line1[index])) word_count++; if (word_count == 1) ////处理第二个单词 &#123; line1[index] = toupper(line1[index]); ////将第二个单词大写 &#125; &#125; cout &lt;&lt; line1 &lt;&lt; endl; &#125; else cout &lt;&lt; "input error" &lt;&lt; endl; return 0;&#125; 若要用下标的方式访问字符，一定要先检查字符串是否为空，否则容易出现不可预知的结果。]]></content>
      <categories>
        <category>c++</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>string</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[hello my blog!]]></title>
    <url>%2Fhello-world.html</url>
    <content type="text"><![CDATA[到这个时候差不多把博客的主体搭建完，很累但很开心，像有了自己的小家一样。谨此留以纪念。 搭建过程搭建基本的网站有很多大佬热心分享他们的文章，现在让我写也不会写的比他们好，就不重复造轮子了，我搭建网站采取的是github page + hexo 。下面我贴出对我搭建网站很有帮助的作者的文章： 基于Hexo+Github+Coding搭建个人博客——基础篇(从菜鸟到放弃) 打造个性超赞博客Hexo+NexT+GitHubPages的超深度优化 我用的博客主题是 nexT,nexT官网教程建议读一读。 更精细的网站打造网站的更多细节，以及第三方应用的集成教程，看基于Hexo搭建个人博客——进阶篇(从入门到入土)。遇到的其他问题自行google了。 “搭”后感？不敢有啥感觉，我觉得我放假这两天除了吃喝拉睡，其他时间都沉迷于搭建网站不可自拔，身体已经严重警告我得睡觉了！就这样～]]></content>
      <categories>
        <category>碎碎念</category>
      </categories>
      <tags>
        <tag>碎碎念</tag>
      </tags>
  </entry>
</search>
